<UserControl x:Class="PetrolPumpMS.App.Views.CustomersView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="20" />
            <ColumnDefinition Width="340" />
        </Grid.ColumnDefinitions>

        <StackPanel Grid.Column="0">
            <Grid Margin="0,0,0,16">
                <TextBlock Text="Customers / Credit Accounts" Style="{StaticResource PageTitleText}" HorizontalAlignment="Left" />
                <Button Content="+ Add Customer" Style="{StaticResource PrimaryButton}" HorizontalAlignment="Right" Command="{Binding StartAddCommand}" />
            </Grid>

            <Border Style="{StaticResource Card}">
                <DataGrid Style="{StaticResource AppDataGrid}" ItemsSource="{Binding Customers}">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Name" Binding="{Binding Name}" Width="140" />
                        <DataGridTextColumn Header="Phone" Binding="{Binding Phone}" Width="120" />
                        <DataGridTextColumn Header="Credit Limit" Binding="{Binding CreditLimit, StringFormat={}Rs. {0:N0}}" Width="120" />
                        <DataGridTextColumn Header="Balance" Binding="{Binding Balance, StringFormat={}Rs. {0:N2}}" Width="120" />
                        <DataGridTemplateColumn Header="Status" Width="100">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <Border Style="{StaticResource DangerBadge}" Visibility="{Binding IsOverLimit, Converter={StaticResource BoolToVisibilityConverter}}">
                                        <TextBlock Text="OVER LIMIT" Style="{StaticResource BadgeText}" Foreground="{StaticResource DangerBrush}" />
                                    </Border>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                        <DataGridTemplateColumn Header="" Width="220">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <StackPanel Orientation="Horizontal">
                                        <Button Content="Record Payment" Style="{StaticResource SecondaryButton}" Padding="8,4" FontSize="11" Margin="0,0,6,0"
                                                Command="{Binding DataContext.StartPaymentCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                CommandParameter="{Binding}" />
                                        <Button Content="Edit" Style="{StaticResource SecondaryButton}" Padding="8,4" FontSize="11" Margin="0,0,6,0"
                                                Command="{Binding DataContext.StartEditCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                CommandParameter="{Binding}" />
                                        <Button Content="Delete" Style="{StaticResource SecondaryButton}" Padding="8,4" FontSize="11"
                                                Command="{Binding DataContext.DeleteCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                CommandParameter="{Binding}" />
                                    </StackPanel>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                    </DataGrid.Columns>
                </DataGrid>
            </Border>
        </StackPanel>

        <!-- Edit panel -->
        <Border Grid.Column="2" Style="{StaticResource Card}" VerticalAlignment="Top"
                Visibility="{Binding IsEditing, Converter={StaticResource BoolToVisibilityConverter}}">
            <StackPanel>
                <TextBlock Text="Customer Details" Style="{StaticResource SectionHeaderText}" Margin="0,0,0,14" />
                <TextBlock Text="Name" Style="{StaticResource FieldLabelText}" />
                <TextBox Style="{StaticResource AppTextBox}" Text="{Binding FormName, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,12" />
                <TextBlock Text="Phone" Style="{StaticResource FieldLabelText}" />
                <TextBox Style="{StaticResource AppTextBox}" Text="{Binding FormPhone, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,12" />
                <TextBlock Text="Address" Style="{StaticResource FieldLabelText}" />
                <TextBox Style="{StaticResource AppTextBox}" Text="{Binding FormAddress, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,12" />
                <TextBlock Text="Credit Limit" Style="{StaticResource FieldLabelText}" />
                <TextBox Style="{StaticResource AppTextBox}" Text="{Binding FormCreditLimitText, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,16" />

                <TextBlock Text="{Binding ErrorMessage}" Style="{StaticResource ValidationErrorText}"
                           Visibility="{Binding ErrorMessage, Converter={StaticResource StringNotEmptyToVisibilityConverter}}" Margin="0,0,0,10" />

                <StackPanel Orientation="Horizontal">
                    <Button Content="Save" Style="{StaticResource PrimaryButton}" Command="{Binding SaveCommand}" Margin="0,0,8,0"
                            IsEnabled="{Binding IsBusy, Converter={StaticResource InverseBoolConverter}}" />
                    <Button Content="Cancel" Style="{StaticResource SecondaryButton}" Command="{Binding CancelEditCommand}" />
                </StackPanel>
            </StackPanel>
        </Border>

        <!-- Payment panel -->
        <Border Grid.Column="2" Style="{StaticResource Card}" VerticalAlignment="Top"
                Visibility="{Binding IsPaying, Converter={StaticResource BoolToVisibilityConverter}}">
            <StackPanel>
                <TextBlock Text="{Binding Selected.Name, StringFormat={}Record Payment — {0}}" Style="{StaticResource SectionHeaderText}" Margin="0,0,0,14" TextWrapping="Wrap" />
                <TextBlock Text="Amount" Style="{StaticResource FieldLabelText}" />
                <TextBox Style="{StaticResource AppTextBox}" Text="{Binding PaymentAmountText, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,12" />
                <TextBlock Text="Note" Style="{StaticResource FieldLabelText}" />
                <TextBox Style="{StaticResource AppTextBox}" Text="{Binding PaymentNote, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,16" />

                <TextBlock Text="{Binding ErrorMessage}" Style="{StaticResource ValidationErrorText}"
                           Visibility="{Binding ErrorMessage, Converter={StaticResource StringNotEmptyToVisibilityConverter}}" Margin="0,0,0,10" />

                <StackPanel Orientation="Horizontal">
                    <Button Content="Save Payment" Style="{StaticResource PrimaryButton}" Command="{Binding RecordPaymentCommand}" Margin="0,0,8,0"
                            IsEnabled="{Binding IsBusy, Converter={StaticResource InverseBoolConverter}}" />
                    <Button Content="Cancel" Style="{StaticResource SecondaryButton}" Command="{Binding CancelPaymentCommand}" />
                </StackPanel>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
