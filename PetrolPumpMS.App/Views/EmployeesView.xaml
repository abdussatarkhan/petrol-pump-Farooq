<UserControl x:Class="PetrolPumpMS.App.Views.EmployeesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="20" />
            <ColumnDefinition Width="340" />
        </Grid.ColumnDefinitions>

        <StackPanel Grid.Column="0">
            <Grid Margin="0,0,0,16">
                <TextBlock Text="Employees" Style="{StaticResource PageTitleText}" HorizontalAlignment="Left" />
                <Button Content="+ Add Employee" Style="{StaticResource PrimaryButton}" HorizontalAlignment="Right" Command="{Binding StartAddCommand}" />
            </Grid>

            <Border Style="{StaticResource Card}">
                <DataGrid Style="{StaticResource AppDataGrid}" ItemsSource="{Binding Employees}">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Name" Binding="{Binding Name}" Width="140" />
                        <DataGridTextColumn Header="Role" Binding="{Binding Role}" Width="110" />
                        <DataGridTextColumn Header="Phone" Binding="{Binding Phone}" Width="120" />
                        <DataGridTextColumn Header="Salary" Binding="{Binding Salary, StringFormat={}Rs. {0:N0}}" Width="110" />
                        <DataGridTextColumn Header="Joined" Binding="{Binding JoinDate, StringFormat=d}" Width="90" />
                        <DataGridTemplateColumn Header="Status" Width="90">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <Border Style="{StaticResource SuccessBadge}">
                                        <TextBlock Text="{Binding Status}" Style="{StaticResource BadgeText}" Foreground="{StaticResource SuccessBrush}" />
                                    </Border>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                        <DataGridTemplateColumn Header="" Width="160">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <StackPanel Orientation="Horizontal">
                                        <Button Content="Edit" Style="{StaticResource SecondaryButton}" Padding="8,4" FontSize="11" Margin="0,0,6,0"
                                                Command="{Binding DataContext.StartEditCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                CommandParameter="{Binding}" />
                                        <Button Content="Delete" Style="{StaticResource SecondaryButton}" Padding="8,4" FontSize="11"
                                                Command="{Binding DataContext.DeleteCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                CommandParameter="{Binding}" />
                                    </StackPanel>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                    </DataGrid.Columns>
                </DataGrid>
            </Border>
        </StackPanel>

        <!-- Edit panel -->
        <Border Grid.Column="2" Style="{StaticResource Card}" VerticalAlignment="Top"
                Visibility="{Binding IsEditing, Converter={StaticResource BoolToVisibilityConverter}}">
            <StackPanel>
                <TextBlock Text="Employee Details" Style="{StaticResource SectionHeaderText}" Margin="0,0,0,14" />

                <TextBlock Text="Name" Style="{StaticResource FieldLabelText}" />
                <TextBox Style="{StaticResource AppTextBox}" Text="{Binding FormName, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,12" />

                <TextBlock Text="Phone" Style="{StaticResource FieldLabelText}" />
                <TextBox Style="{StaticResource AppTextBox}" Text="{Binding FormPhone, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,12" />

                <TextBlock Text="Address" Style="{StaticResource FieldLabelText}" />
                <TextBox Style="{StaticResource AppTextBox}" Text="{Binding FormAddress, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,12" />

                <TextBlock Text="Role" Style="{StaticResource FieldLabelText}" />
                <ComboBox Style="{StaticResource AppComboBox}" ItemsSource="{Binding Roles}" SelectedItem="{Binding FormRole}" Margin="0,0,0,12" />

                <TextBlock Text="Salary" Style="{StaticResource FieldLabelText}" />
                <TextBox Style="{StaticResource AppTextBox}" Text="{Binding FormSalaryText, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,12" />

                <TextBlock Text="Join Date" Style="{StaticResource FieldLabelText}" />
                <DatePicker SelectedDate="{Binding FormJoinDate}" Margin="0,0,0,12" />

                <TextBlock Text="Status" Style="{StaticResource FieldLabelText}" />
                <ComboBox Style="{StaticResource AppComboBox}" ItemsSource="{Binding Statuses}" SelectedItem="{Binding FormStatus}" Margin="0,0,0,16" />

                <TextBlock Text="{Binding ErrorMessage}" Style="{StaticResource ValidationErrorText}"
                           Visibility="{Binding ErrorMessage, Converter={StaticResource StringNotEmptyToVisibilityConverter}}" Margin="0,0,0,10" />

                <StackPanel Orientation="Horizontal">
                    <Button Content="Save" Style="{StaticResource PrimaryButton}" Command="{Binding SaveCommand}" Margin="0,0,8,0"
                            IsEnabled="{Binding IsBusy, Converter={StaticResource InverseBoolConverter}}" />
                    <Button Content="Cancel" Style="{StaticResource SecondaryButton}" Command="{Binding CancelEditCommand}" />
                </StackPanel>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
